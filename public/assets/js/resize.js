window.addEventListener('resize', function() {
    if (window.innerWidth < 900) {
        alert('You need to use a device with a screen larger than 900px, like an iPad or a laptop.');
    }
});

if (window.innerWidth < 900) {
    alert('You need to use a device with a screen larger than 900px, like an iPad or a laptop.');
}
